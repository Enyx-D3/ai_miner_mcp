import http from "node:http";import {Brain2RuntimeWorker} from "./worker.mjs";
function json(res,status,payload){const body=JSON.stringify(payload);res.writeHead(status,{"content-type":"application/json","content-length":Buffer.byteLength(body),"cache-control":"no-store"});res.end(body)}
async function readJson(req,maxBytes=1024*1024){const chunks=[];let size=0;for await(const chunk of req){size+=chunk.length;if(size>maxBytes)throw new Error("REQUEST_TOO_LARGE");chunks.push(chunk)}return JSON.parse(Buffer.concat(chunks).toString("utf8")||"{}");}
export function createRuntimeServer({worker=new Brain2RuntimeWorker(),host="127.0.0.1",port=8791}={}){const server=http.createServer(async(req,res)=>{try{if(req.method==="GET"&&req.url==="/v1/health")return json(res,200,{ok:true,...worker.manifest()});if(req.method==="POST"&&req.url==="/v1/jobs"){const request=await readJson(req);const result=await worker.execute(request);return json(res,result.state==="DENIED"?403:result.state==="FAILED"?500:200,result)}return json(res,404,{ok:false,error:"NOT_FOUND"});}catch(error){return json(res,400,{ok:false,error:error instanceof Error?error.message:String(error)})}});return{server,listen:()=>new Promise((resolve,reject)=>{server.once("error",reject);server.listen(port,host,()=>resolve({host,port}))}),close:()=>new Promise((resolve,reject)=>server.close((err)=>err?reject(err):resolve()))};}
